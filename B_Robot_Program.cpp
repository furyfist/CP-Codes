#include <bits/stdc++.h>
using namespace std;

int main(){
    int tc; cin >> tc;
    while(tc--){
        int n,x,k; cin >> n >> x >> k;
        string s; cin >> s;
        int cnts =s.size(), cntk = k,ans =0;
        while (cnts !=0 || cntk !=0){
            if(x==0){
                cnts=n; 
                ans++;
            }
            if()
        }
    }
}