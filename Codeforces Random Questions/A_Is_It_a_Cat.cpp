#include <bits/stdc++.h>
using namespace std;

#define endl '\n'
#define int long long

const int MOD = 1e9 + 7;
const int INF = LLONG_MAX >> 1;

signed main() {
    ios::sync_with_stdio(false); cin.tie(NULL);
    int tc; cin >> tc;
    string s2;
    while (tc--) {
        int n; cin >> n;
        string s; cin >> s;
        for(int i=0; i<n; i++){
           if(s[i] >= 97) s[i] - 26;
        }
        for(int j=0; j<n; j++){
            if(s[j] == 'M'){
                if

            }
        }
    }
}